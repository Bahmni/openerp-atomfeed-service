{
    "id": $id,
    "params": {
        "data": {
            #foreach( $param in $parametersList )
                "$param.name": "$param.value"#if($foreach.hasNext),#end
            #end
        }
    }
}